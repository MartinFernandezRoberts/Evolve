import { PhaseStaticScene } from './phase-static-scene.js';

/** Escena de asentamiento inicial antes de que existan estructuras urbanas construidas. */
export class EarlySettlementScene extends PhaseStaticScene {
    constructor() {
        super('early-settlement');
    }
}
